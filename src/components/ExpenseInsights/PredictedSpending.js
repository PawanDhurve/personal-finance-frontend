import React from 'react';

const PredictedSpending = ({ predictedNextMonthSpending }) => {
    return <p><strong>Predicted Next Month:</strong> ${predictedNextMonthSpending}</p>;
};

export default PredictedSpending;
